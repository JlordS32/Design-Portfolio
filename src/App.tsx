import Button from "./components/Button";

const App = () => {
   return (
      <main>
         <Button buttonType="primary"/>
         <Button buttonType="secondary"/>
      </main>
   );
};

export default App;
